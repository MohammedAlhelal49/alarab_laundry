<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="point_of_sale.NumberPopup" t-inherit-mode="extension">
        <xpath expr="//div[contains(@class,'input-symbol')]" position="replace">
            <t t-if="props.showAmountInput">
                <input type="number"
                       class="input-symbol form-control form-control-lg text-center mb-3"
                       t-att-value="state.buffer"
                       t-on-input="onPercentInput"/>
            </t>
            <t t-else="">
                <div class="input-symbol mb-3">
                    <div class="popup-input value active form-control form-control-lg text-center">
                        <span t-if="this.state.buffer || !props.placeholder"
                              class="input-value"
                              t-esc="props.formatDisplayedValue(this.state.buffer || '')"/>
                        <span t-else=""
                              class="text-muted"
                              t-esc="props.placeholder"/>
                    </div>
                </div>
            </t>

            <div class="mb-3 mt-3" t-if="props.showAmountInput">
                <label class="form-label">Amount</label>
                <input type="number"
                       step="0.01"
                       class="form-control form-control-lg text-center"
                       t-att-value="state.discountAmount"
                       t-att-disabled="!props.subtotal"
                       t-on-input="onAmountInput"/>
            </div>
        </xpath>
    </t>
</templates>