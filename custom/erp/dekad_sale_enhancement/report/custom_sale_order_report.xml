<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_report_custom_sale_order" model="ir.actions.report">
        <field name="name">Custom Sale Order</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">dekad_sale_enhancement.report_custom_sale_order_document</field>
        <field name="report_file">dekad_sale_enhancement.report_custom_sale_order_document</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <template id="report_custom_sale_order_document">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="web.external_layout" t-lang="doc.partner_id.lang">
                    <t t-set="doc" t-value="doc.with_context(lang=doc.partner_id.lang)" />
                    <t t-set="forced_vat" t-value="doc.fiscal_position_id.foreign_vat"/>

                    <!-- ================= HEADER: Title + Document/Customer info ================= -->
                    <div class="text-center mb-4">
                        <h3 class="mb-0" style="direction:rtl;">طلب بيع</h3>
                        <h3 class="mt-0">Sale Order</h3>
                    </div>

                    <div class="row mb-4">
                        <div class="col-6 ps-3" name="custom_sale_customer_info_block">

                            <!-- Customer -->
                            <p class="mb-1">
                                <strong>Customer :</strong>
                                <span t-esc="doc.partner_id.name"/>
                            </p>

                            <!-- Customer Mobile -->
                            <p class="mb-1" t-if="doc.partner_id.mobile or doc.partner_id.phone">
                                <strong>Customer Mobile :</strong>

                                <span t-if="doc.partner_id.mobile"
                                      t-esc="doc.partner_id.mobile"/>

                                <t t-if="doc.partner_id.mobile and doc.partner_id.phone">
                                    /
                                </t>

                                <span t-if="doc.partner_id.phone"
                                      t-esc="doc.partner_id.phone"/>
                            </p>

                            <!-- Customer TRN -->
                            <p class="mb-1" t-if="doc.partner_id.vat">
                                <strong>Customer TRN :</strong>
                                <span t-esc="doc.partner_id.vat"/>
                            </p>

                            <!-- Customer Address -->
                            <p class="mb-1"
                               t-if="doc.partner_id.street
                                     or doc.partner_id.street2
                                     or doc.partner_id.city
                                     or doc.partner_id.state_id
                                     or doc.partner_id.country_id">

                                <strong>Customer Address :</strong>

                                <span t-if="doc.partner_id.street"
                                      t-esc="doc.partner_id.street"/>

                                <t t-if="doc.partner_id.street2">
                                    <t t-if="doc.partner_id.street">, </t>
                                    <span t-esc="doc.partner_id.street2"/>
                                </t>

                                <t t-if="doc.partner_id.city">
                                    <t t-if="doc.partner_id.street or doc.partner_id.street2">, </t>
                                    <span t-esc="doc.partner_id.city"/>
                                </t>

                                <t t-if="doc.partner_id.state_id">
                                    <t t-if="doc.partner_id.street or doc.partner_id.street2 or doc.partner_id.city">, </t>
                                    <span t-esc="doc.partner_id.state_id.name"/>
                                </t>

                                <t t-if="doc.partner_id.country_id">
                                    <t t-if="doc.partner_id.street
                                             or doc.partner_id.street2
                                             or doc.partner_id.city
                                             or doc.partner_id.state_id">, </t>
                                    <span t-esc="doc.partner_id.country_id.name"/>
                                </t>

                            </p>

                        </div>


                        <!-- LEFT: Document information, label on the left of the value -->
                        <div class="col-6 pe-3" name="custom_sale_document_info_block">

                            <!-- Order Number -->
                            <p class="mb-1" t-if="doc.name">
                                <t t-if="doc.state in ['draft', 'sent']">
                                    <strong>Quotation No :</strong>
                                </t>
                                <t t-else="">
                                    <strong>Order No :</strong>
                                </t>
                                <span t-field="doc.name"/>
                            </p>

                            <!-- Order Date -->
                            <p class="mb-1" t-if="doc.date_order">
                                <t t-if="doc.state in ['draft', 'sent']">
                                    <strong>Quotation Date :</strong>
                                </t>
                                <t t-else="">
                                    <strong>Order Date :</strong>
                                </t>
                                <span t-field="doc.date_order" t-options='{"widget": "date"}'/>
                            </p>

                            <!-- Expiration -->
                            <p class="mb-1" t-if="doc.validity_date and doc.state in ['draft', 'sent']">
                                <strong>Expiration :</strong>
                                <span t-field="doc.validity_date"/>
                            </p>

                            <!-- Salesperson -->
                            <p class="mb-1" t-if="doc.user_id.name">
                                <strong>Salesperson :</strong>
                                <span t-field="doc.user_id.name"/>
                            </p>

                            <!-- Your Reference -->
                            <p class="mb-1" t-if="doc.client_order_ref">
                                <strong>Your Reference :</strong>
                                <span t-field="doc.client_order_ref"/>
                            </p>

                        </div>

                    </div>

                    <div class="page">
                        <div class="oe_structure"/>

                        <!-- Is there a discount on at least one line? -->
                        <t t-set="lines_to_report" t-value="doc._get_order_lines_to_report()"/>
                        <t t-set="display_discount" t-value="any(l.discount for l in lines_to_report)"/>

                        <!-- Lines whose product is flagged as a discount product are
                             excluded from the items table and totalled separately -->
                        <t t-set="discount_lines" t-value="lines_to_report.filtered(lambda l: not l.display_type and l.product_type != 'combo' and l.product_id.is_discount)"/>
                        <t t-set="discount_total" t-value="sum(discount_lines.mapped('price_subtotal'))"/>

                        <div class="oe_structure"></div>
                        <table class="o_has_total_table table o_main_table table-borderless">
                            <!-- In case we want to repeat the header, remove "display: table-row-group" -->
                            <thead style="display: table-row-group">
                                <tr>
                                    <th name="th_index" class="text-start">#</th>
                                    <th name="th_description" class="text-start">Description</th>
                                    <th name="th_quantity" class="text-end text-nowrap">Quantity</th>
                                    <th name="th_priceunit" class="text-end text-nowrap">Unit Price</th>
                                    <th name="th_discount" t-if="display_discount" class="text-end">
                                        <span>Disc.%</span>
                                    </th>
                                    <th name="th_taxes" class="text-end">Taxes</th>
                                    <th name="th_subtotal" class="text-end">
                                        <span>Amount</span>
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="sale_tbody">

                                <t t-set="current_subtotal" t-value="0"/>
                                <!-- discount lines removed from the set displayed in the table -->
                                <t t-set="lines" t-value="lines_to_report - discount_lines"/>
                                <t t-set="line_counter" t-value="0"/>

                                <t t-foreach="lines" t-as="line">

                                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                                    <t t-if="not line.display_type and line.product_type != 'combo'" t-set="line_counter" t-value="line_counter + 1"/>

                                    <tr
                                        t-att-class="'fw-bold o_line_section' if (
                                            line.display_type == 'line_section'
                                            or line.product_type == 'combo'
                                        )
                                        else 'fst-italic o_line_note' if line.display_type == 'line_note'
                                        else ''"
                                    >
                                        <t t-if="not line.display_type and line.product_type != 'combo'">
                                            <td name="td_index" class="text-start">
                                                <span t-esc="line_counter">1</span>
                                            </td>
                                            <td name="td_name"><span t-field="line.name">Bacon Burger</span></td>
                                            <td name="td_quantity" t-attf-class="text-end {{ 'text-nowrap' if (not line.product_packaging_id or len(line.product_packaging_id.name) &lt; 10) else '' }}">
                                                <span t-field="line.product_uom_qty">3</span>
                                                <span t-field="line.product_uom">units</span>
                                                <span t-if="line.product_packaging_id">
                                                    (<span t-field="line.product_packaging_qty" t-options='{"widget": "integer"}'/> <span t-field="line.product_packaging_id"/>)
                                                </span>
                                            </td>
                                            <td name="td_priceunit" class="text-end text-nowrap">
                                                <span t-field="line.price_unit">3</span>
                                            </td>
                                            <td t-if="display_discount" class="text-end">
                                                <span t-field="line.discount">-</span>
                                            </td>
                                            <t t-set="taxes" t-value="', '.join([(tax.invoice_label or tax.name) for tax in line.tax_id])"/>
                                            <td name="td_taxes" t-attf-class="text-end {{ 'text-nowrap' if len(taxes) &lt; 10 else '' }}">
                                                <span t-out="taxes">Tax 15%</span>
                                            </td>
                                            <td t-if="not line.is_downpayment" name="td_subtotal" class="text-end o_price_total">
                                                <span t-field="line.price_subtotal">27.00</span>
                                            </td>
                                        </t>
                                        <t t-elif="line.display_type == 'line_section' or line.product_type == 'combo'">
                                            <td name="td_section_line" colspan="99">
                                                <span t-field="line.name">A section title</span>
                                            </td>
                                            <t t-set="current_section" t-value="line"/>
                                            <t t-set="current_subtotal" t-value="0"/>
                                        </t>
                                        <t t-elif="line.display_type == 'line_note'">
                                            <td name="td_note_line" colspan="99">
                                                <span t-field="line.name">A note, whose content usually applies to the section or product above.</span>
                                            </td>
                                        </t>
                                    </tr>

                                    <t
                                        t-if="current_section and (
                                            line_last
                                            or lines[line_index+1].display_type == 'line_section'
                                            or lines[line_index+1].product_type == 'combo'
                                            or (
                                                line.combo_item_id
                                                and not lines[line_index+1].combo_item_id
                                            )
                                        ) and not line.is_downpayment"
                                    >
                                        <t t-set="current_section" t-value="None"/>
                                        <tr class="is-subtotal text-end">
                                            <td name="td_section_subtotal" colspan="99">
                                                <strong class="mr16">Subtotal</strong>
                                                <span
                                                    t-out="current_subtotal"
                                                    t-options='{"widget": "monetary", "display_currency": doc.currency_id}'
                                                >31.05</span>
                                            </td>
                                        </tr>
                                    </t>
                                </t>
                            </tbody>
                        </table>
                        <div class="clearfix" name="so_total_summary">
                            <div id="total" class="row mt-n3" name="total">
                                <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                                    <table class="o_total_table table table-borderless">

                                        <!-- Discount row (from products flagged is_discount = True),
                                             shown before the Untaxed Amount row -->
                                        <tr class="o_discount" t-if="discount_lines">
                                            <td>
                                                <span>Discount</span>
                                            </td>
                                            <td class="text-end">
                                                <span t-out="discount_total"
                                                      t-options='{"widget": "monetary", "display_currency": doc.currency_id}'
                                                >-4.00</span>
                                            </td>
                                        </tr>

                                        <!-- Tax totals -->
                                        <t t-call="sale.document_tax_totals">
                                            <t t-set="tax_totals" t-value="doc.tax_totals"/>
                                            <t t-set="currency" t-value="doc.currency_id"/>
                                        </t>
                                    </table>
                                </div>
                            </div>
                        </div>

                        <div class="row"
                             t-if="doc.company_id.sale_amount_total_words and doc.amount_total">
                            <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                                <div class="mb-2">
                                    <p class="text-start lh-sm">
                                        Total amount in words:<br/>
                                        <small class="text-muted lh-sm">
                                            <span t-esc="doc.currency_id.amount_to_text(doc.amount_total)"/>
                                        </small>
                                    </p>
                                </div>
                            </div>
                        </div>
                        <div class="oe_structure"></div>

                        <div t-if="not doc.signature" class="oe_structure"></div>
                        <div t-else="" class="mt-4 ml64 mr4" name="signature">
                            <div class="offset-8">
                                <strong>Signature</strong>
                            </div>
                            <div class="offset-8">
                                <img t-att-src="image_data_uri(doc.signature)" style="max-height: 4cm; max-width: 8cm;"/>
                            </div>
                            <div class="offset-8 text-center">
                                <span t-field="doc.signed_by">Oscar Morgan</span>
                            </div>
                        </div>
                        <div>
                            <span t-field="doc.note" t-attf-style="#{'text-align:justify;text-justify:inter-word;' if doc.company_id.terms_type != 'html' else ''}" name="order_note"/>
                            <p t-if="not is_html_empty(doc.payment_term_id.note)">
                                <span t-field="doc.payment_term_id.note">The payment should also be transmitted with love</span>
                            </p>
                            <div class="oe_structure"/>
                            <p t-if="doc.fiscal_position_id and not is_html_empty(doc.fiscal_position_id.sudo().note)"
                                id="fiscal_position_remark">
                                <strong>Fiscal Position Remark:</strong>
                                <span t-field="doc.fiscal_position_id.sudo().note">No further requirements for this payment</span>
                            </p>
                        </div>
                        <div class="oe_structure"/>
                    </div>
                </t>
            </t>
        </t>
    </template>

</odoo>