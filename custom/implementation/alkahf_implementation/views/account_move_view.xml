<odoo>
    <record id="view_move_form_inherit_line_order" model="ir.ui.view">
        <field name="name">account.move.form.line.order</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='invoice_line_ids']//field[@name='price_subtotal']"
                   position="after">

                <field name="price_total"
                       string="Amount With VAT"
                       optional="show"/>

            </xpath>
            <xpath expr="//field[@name='name']" position="after">

                <field name="serial_number"
                       readonly="1"
                       nolabel="1"
                       class="oe_inline text-muted"
                       style="font-size: 14px; margin-left: 3px; position: relative;top: -8px;"/>

            </xpath>

        </field>
    </record>
    <record id="view_move_tree_inherit_serial"
        model="ir.ui.view">

        <field name="name">account.move.tree.serial</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_tree"/>

        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']"
                   position="after">

                <field name="serial_number"
                       optional="show"/>

            </xpath>

        </field>

    </record>

    <record id="view_account_move_filter_inherit_serial"
        model="ir.ui.view">

        <field name="name">account.move.search.serial</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>

        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']"
                   position="after">

                <field name="serial_number"
                       string="Serial"/>

            </xpath>

        </field>

    </record>

</odoo>