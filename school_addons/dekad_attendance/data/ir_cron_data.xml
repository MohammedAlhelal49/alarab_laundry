<?xml version="1.0" encoding="utf-8"?>
<odoo>
   <record id="cron_create_attendance_sheet" model="ir.cron">
            <field name="name">Create attendance sheet for each classroom in each day</field>
            <field name="model_id" ref="dekad_attendance.model_de_attendance_sheet"/>
            <field name="type">ir.actions.server</field>
            <field name="state">code</field>
            <field name="code">model.cron_create_attendance_sheet()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>

        </record>
</odoo>
