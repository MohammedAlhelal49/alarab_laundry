<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit User Form View -->
    <record id="view_users_form_mobile_apps" model="ir.ui.view">
        <field name="name">res.users.form.mobile.apps</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Mobile Apps Access" name="mobile_apps_access">
                    <group>
                        <field name="allowed_mobile_app_ids" widget="many2many_tags" placeholder="Select allowed mobile apps..."/>
                    </group>
                    <p class="text-muted">
                        Explicitly authorize specific mobile applications or features for this user.
                        If a user has permissions via both security groups and direct user selection, they will have full access.
                    </p>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
